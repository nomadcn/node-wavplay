#include "MacPlayer.h"

MacPlayer::MacPlayer()
{
}

MacPlayer::~MacPlayer()
{
}

int MacPlayer::Open(const Player::Param* param)
{
    LOGD_CALL(TAG, "start");
    LOGD_CALL(TAG, "end");

    return 0;
}

int MacPlayer::Start(const int loop)
{
    LOGD_CALL(TAG, "start");
    LOGD_CALL(TAG, "end");
    return 0;
}

int MacPlayer::Stop()
{
    LOGD_CALL(TAG, "start");
    LOGD_CALL(TAG, "end");
    return 0;
}

int MacPlayer::Close()
{
    LOGD_CALL(TAG, "start");
    LOGD_CALL(TAG, "end");
    return 0;
}
